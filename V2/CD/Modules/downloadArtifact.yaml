parameters:
  - name: pipelineResource
    type: string
  - name: pipelineSource
    type: string
  - name: packageName
    type: string

steps:
  - download: ${{ parameters.pipelineResource }}
    artifact: ${{ parameters.packageName }}
    displayName: 'Download ${{ parameters.packageName }} from ${{ parameters.pipelineResource }}'
