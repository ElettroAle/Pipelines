# ── Preview test pipeline: publish-dotNet (GitFlow) ───────────────────────────
#
# Usato con: az pipelines run --preview
#
# Scopo: verificare che il template V3/CI/GitFlow/publish-dotNet.yaml si espanda
# correttamente senza errori di sintassi ADO, variabili non risolte o parametri
# obbligatori mancanti.
#
# Prerequisiti:
#   - Pipeline registrata in ADO: "Pipelines - Preview publish-dotNet"
#   - Variable group 'development' linkato alla pipeline
#
# Esecuzione:
#   az pipelines run --name "Pipelines - Preview publish-dotNet" --preview
# ──────────────────────────────────────────────────────────────────────────────

trigger: none
pr: none

resources:
  repositories:
    - repository: self
      type: git
      name: Demetra/DPS.Demetra.Infrastructure

extends:
  template: V3/CI/GitFlow/publish-dotNet.yaml@self
  parameters:
    projectName: 'PreviewTest'
